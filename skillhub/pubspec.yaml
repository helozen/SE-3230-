name: skillhub
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2  # For API communication
  google_maps_flutter: ^2.2.0  # For map features
  flutter_local_notifications: ^15.0.0  # For notifications
  cupertino_icons: ^1.0.8  # For iOS-style icons

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
